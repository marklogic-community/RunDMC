<!-- Legal version numbers of the Server that we have documentation for -->
<!-- When you change the default version, you'll need to re-generate the
     TOC for the new default version. -->
<versions>
  <version number="4.2"/>
  <version number="5.0"/>
  <version number="6.0"
           display="MarkLogic 6"
           default="yes"/>       <!-- what we serve when the request URL
                                      does not explicitly specify a version -->
</versions>
